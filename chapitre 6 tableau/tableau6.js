/*function rechercher(tableau, v) {
    return tableau.includes(v);
}

let tableau = [1, 2, 3, 4, 5];
console.log(rechercher(tableau, 3)); 
console.log(rechercher(tableau, 6));
*/

 const rechercher =() => {

let arr = [1, 2, 3, 4, 5];
let isIncluded = arr.includes(77); // Vérifie si le tableau contient 2
console.log(isIncluded); 

 }
rechercher ();